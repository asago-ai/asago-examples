[project]
name = "asago-examples"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
policy-mapper = [
    "asago-policy-mapper-examples",
]

[tool.uv.sources]
asago-policy-mapper-examples = { path = "./asago-policy-mapper" }
